---
name: stock-analog-fit
description: 读取股票历史日K行情，做趋势拟合（回归通道、多项式、均线、支撑/阻力线、突破判断），在另一只（或同一只）股票的历史里找形态最相似的区间并叠图对比，或在一篮子股票里筛出走势最相似的股票，按相似区间或指定历史行情（如“最乐观/最悲观那段”）推演未来路径、目标价和时间。只要用户提到 K线/走势/趋势线/画线/拟合/回归通道/支撑阻力、“拿 A 的历史走势类比 B”、“找相似区间/相似形态”、“找跟某股走势最像的股票”、“按某股某年那波行情推演”、“两条线画在一张图上对比”、“最乐观/最悲观情景目标价”，或给出股票代码并要求看趋势、画图、分析历史行情，都应使用本技能——即使用户没说“拟合”或“相似区间”这些词。Use for stock price history charting, trendline/regression fitting, historical analog (pattern similarity) matching between tickers, multi-ticker similarity screening, and scenario path projection.
---

# 股票 K 线拟合 · 历史相似区间 · 情景推演

四个脚本覆盖四类需求，都在 `scripts/` 下，共用 `common.py`（取数、中文字体、对数坐标）：

| 用户想要 | 脚本 | 典型说法 |
|---|---|---|
| 看一只或几只股票自己的趋势 | `kline_fit.py` | “画 NIO 的 K 线拟合找趋势” |
| 用 A 的历史相似区间推 B 的未来 | `analog.py` | “拿 NIO 过往相似区间分析 SECZ，两条线画一张图” |
| 按 A 某段指定行情（最乐观/悲观）推演 B | `scenario.py` | “按 NIO 2020 年 6 月到最高点 66.99 的曲线拟合 SECZ” |
| 没指定参照股，要在一批股票里找最像的 | `screen.py` | “找跟 SECZ K 线走势最相似的股票，分析未来走势” |

用户常常从第一类开始，再逐步追问到后面几类，每一步都复用前一步的数据和结论。

用户只给了一只参照股时，可以提醒他：单一参照股的样本偏差很大。SECZ 的实例里，只用 NIO 得到 40 天中位 −35%，换成 65 只股票筛选后中位约 −1%。建议再用 `screen.py` 做交叉验证。

用户想要可视化界面时，仓库根目录的 `app.py`（Streamlit）已经把四个功能都做成了网页：`streamlit run app.py`。

## 第 0 步：环境和数据（最容易卡住的地方）

```bash
pip install -q yfinance pandas numpy matplotlib scipy   # macOS 用户本机没有 pip/python 时用 python3 -m venv + python3
curl -s -o /dev/null -w '%{http_code}\n' -m 15 https://query1.finance.yahoo.com/v8/finance/chart/NIO
```

- 返回 `000` / 代理 403 → 网络策略拦截。告诉用户需要放行的域名：`query1.finance.yahoo.com`、`query2.finance.yahoo.com`、`fc.yahoo.com`、`guce.yahoo.com`（云环境：标题栏环境菜单 → 编辑 → Network access），或让用户上传 CSV（Yahoo Finance → Historical Data → Download），用 `--csv / --target-csv / --ref-csv` 跑。
- 返回 `429` 是 Yahoo 限流，说明网络已通，重试即可。
- **拿不到真实数据时绝不用模拟数据冒充结论。** 可以先写好/测好脚本，但要明确说明“这是模拟数据”。

**核对代码。** 不认识的代码（新股、SPAC 合并后改名、OTC）先搜索确认上市日期和交易所。SPAC 合并前的历史属于空壳（价格锚定约 $10 信托价值），不代表公司业务，只用合并后的数据。

## 第 1 步：单股趋势 —— `kline_fit.py`

```bash
cd <skill>/scripts && python kline_fit.py NIO SECZ --period 2y --outdir <输出目录>
```

输出每只股票一张 K 线图（红涨绿跌、对数坐标）和一段文字摘要：整体回归（年化、R²、±2σ 通道位置 z）、近期回归、三次多项式末端方向、均线排列、支撑/阻力线、形态、突破/破位信号。

解读要点：
- **R² 很低**（<0.2）说明整段没有单一方向（震荡或 V 形），此时整体回归的“年化”没有意义，应以近期趋势为准，并向用户说明原因。
- **上市不足半年**时脚本会自动报“区间涨幅”而不是年化。把 30 天涨 195% 外推成年化会得到几十万个百分点，毫无意义。
- 突破后价格远离均线（如偏离 MA20 超过 50%）时，要提醒回调风险，并给出几个可参考的回撤位（MA10、原阻力线）。

## 第 2 步：历史相似区间 —— `analog.py`

```bash
python analog.py SECZ NIO --outdir <输出目录>                 # 目标最近 60 日 vs 参照全历史
python analog.py TSLA TSLA --window 40                        # 自身历史类比，自动排除当前窗口
```

方法（向用户解释时用大白话）：
- **比形状**：两段对数价格标准化后的相关系数。
- **比幅度**：振幅比须在 0.6~1.6 之间。否则只有小波动的平淡区间也会凭形状排到前面，推演会严重偏保守。这是实际踩过的坑：纯按 RMSE 或纯按相关系数排，前几名全是“形状像但幅度只有三分之一”的区间。
- **时间伸缩**：允许参照股用 0.5~3 倍的天数走完同一形态（`--scales`）。急涨急跌的新股，在老股票历史里很少有同速的对应段。
- 取互不重叠的前 5 段，把它们之后 40 日的走势换算成目标股的参考价，报告中位数和上涨占比。

输出：
- 上下两张图的 PNG：上图是目标与最佳匹配叠在一起（左轴目标价格，右轴参照股当时的真实价格，虚线为推演）；下图是 5 段相似区间的后续走势和中位路径。
- CSV 明细表。

报告时：
- 列出每段的日期、用时、相关系数，以及之后 5/10/20/40 日的涨跌。
- 说明每段对应当时发生了什么（例如 2024 年 9 月中国刺激政策），因为同样的形态背后驱动因素可能完全不同。
- 讲清样本少（只有 5 段、来自同一只股票）的局限。

## 第 2b 步：多股票筛选 —— `screen.py`

```bash
python screen.py SECZ --outdir <输出目录>                        # 默认约 65 只候选（加密/金融科技/电动车/AI/量子/新股/meme）
python screen.py SECZ --universe COIN,HOOD,CRCL,BKKT --top 8     # 自定义候选池
python screen.py SECZ --window 30                                 # 只看最近 30 日这段
```

算法和 `analog.py` 相同（复用它的 `match` 和 `path`）。脚本用 yfinance 一次批量下载全部候选股票，约 15 秒跑完。每只股票最多取 2 段（`--per-ticker`），避免一只股票占满名单；再汇总相关系数不低于 0.75 的前 10 段。

输出：
- 上图：最相似的 5 段（来自不同股票）和目标股叠在一起，虚线是它们之后的走势；
- 下图：10 段后续走势的分布（25%~75% 区间、最小到最大、中位路径）；
- CSV：每只股票的最佳匹配。

报告时：
- 列出前几名股票、区间日期和当时发生了什么，再给 5/10/20/40 日的中位数、四分位范围和上涨占比；
- **结果分化时直说方向不明**，不要硬挑一个方向；
- 把继续上涨的几段和回落的几段各自的驱动因素对比出来（新催化剂还是情绪退潮），告诉用户目标股接下来该盯什么。

要主动说明的局限：
- **窗口太短会失去区分度**：一段近乎单边的急涨（比如 30 天）和任何急涨的相关系数都在 0.97 以上，结论不可靠。以包含起伏的完整形态（如上市以来的 V 形）为准，短窗口只作参考。
- **候选池有幸存者偏差**：默认候选池都是后来出名的大波动股票，大涨后崩盘、退市、被遗忘的股票没有进来，结果会偏乐观。

## 第 3 步：指定情景 —— `scenario.py`

```bash
# 乐观：参照股某段上涨行情（取区间最高价作终点）
python scenario.py SECZ NIO --path-start 2020-06-01 --path-end 2021-01-11 --outdir <输出目录>
# 悲观：某段下跌行情（取区间最低价）
python scenario.py SECZ NIO --path-start 2021-01-11 --path-end 2022-05-15 --direction down
```

用户往往只给“从某月到最高点”，最高点的日期和价格需要自己从数据里确认，比如 NIO 在 2021-01-11 盘中最高 66.99。

关键在**锚点**，也就是“目标股今天相当于参照股哪一天”。脚本在路径前半段逐日搜索形状最像、幅度相当的锚点，然后按价格比例和时间伸缩，把锚点到极值点的路径映射到目标股的未来。

输出包括：情景目标价（倍数）、到达时间、途中最大回撤（或反弹）、各倍数节点的大致日期，以及**锚点敏感性表**。

报告时必须做到：
- **给区间而不是单点**。锚点选在不同日期，目标价可能相差一倍（SECZ 实例中为 76~158），把敏感性表里的几档都告诉用户。
- **相关系数低于 0.6 时**脚本会给出警告，这时要明确说明“当前形态和这段行情并不像，只是照搬路径的假设”。
- **说明这段历史行情的前提**。例如 NIO 2020 年的 9 倍涨幅，背后是政府注资、交付量大增、电动车板块整体暴涨和零利率。要让用户看到，复制这条路径需要什么条件。
- 提醒路径中途的回撤，说明按这个剧本持有需要承受什么波动。
- 把它和第 2 步的相似区间结果放在一起比较，比如“这是 5 个相似情景里唯一继续大涨的一个”，避免用户只看到最乐观的那一个。

## 交付

- 发图之前先用 Read 查看 PNG：确认坐标刻度有数字、中文正常显示、线没有错位。
- 用文件发送工具（如 SendUserFile）把图发给用户。
- 结论用表格加几条要点，语言跟随用户（中文用户用中文）。
- 结尾注明“这是基于历史走势的技术面情景推演，不是预测，也不构成投资建议”，一句话即可，不必反复强调。

## 自检清单（踩过的坑）

- 自己临时写的探索代码，要核对实际取到的数据长度是否等于预期窗口。曾因包装函数没把 `period` 参数传给 yfinance，只取到约 1 个月数据，得出了错误的高相关结论。
- 描述多项式末端时，写清“末端向上/向下”加“加速/放缓”，不要只写“趋势在加速”。末端方向和回归方向相反时，这种写法会误导。
- 对数坐标要用 `common.fmt_log_axis`，否则会出现 `6×10⁰` 这类刻度，或者整段刻度都不显示。
- 时间伸缩后映射到日期要插值（脚本已处理），直接取整会让曲线出现锯齿。
